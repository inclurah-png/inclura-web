function CreateStory() {
  return (
    <button
      style={{
        background: "#38bdf8",
        color: "white",
        border: "none",
        padding: "12px 18px",
        borderRadius: "12px",
        cursor: "pointer",
        marginBottom: "16px",
      }}
    >
      + Add Story
    </button>
  );
}

export default CreateStory;
